---
description: Create tickets and a prioritized roadmap from a PRD (and optional design file) - $ARGUMENTS = <prd-path> [<design-path>]
---
Parse $ARGUMENTS into a PRD path (required) and an optional design-file path.
Invoke the factory-roadmap skill with them. Follow it exactly; it owns
extraction, brain seeding, batch triage, and writing docs/factory/roadmap.md.
